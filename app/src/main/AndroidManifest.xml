<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.co.ezenac.jun0397.flower">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- 위치정보활용시 추가 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- 위치정보활용시 추가 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- WIFI 상태활용시 추가 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    `

    <!-- WIFI 상태활용시 추가 -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".makeFlower.MakeFlower" />
        <activity android:name=".makeFlower.Make_Flower_Fragment1" />
        <activity android:name=".makeFlower.Make_Flower_Fragment2" />
        <activity android:name=".makeFlower.Make_Flower_Fragment3" />
        <activity android:name=".login_register.Login" />
        <activity android:name=".login_register.Regist" />
        <activity android:name=".login_register.PhoneNumberCheck" />
        <activity android:name=".find_shop_goods.Find_shop_goods" />
        <activity android:name=".shop.Shop" />
        <activity android:name=".shop.Shop_Fragment1" />
        <activity android:name=".shop.Shop_Fragment2" />
        <activity android:name=".shop.Shop_Fragment3"></activity>
    </application>

</manifest>